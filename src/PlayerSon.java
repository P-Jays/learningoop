public class PlayerSon extends PlayerDad {

    PlayerSon(String name) {
        super(name);
        // untuk bisa meletakkan namanya harus pakai super, agar name nya di lempar ke
        // kelas parent
    }
}
